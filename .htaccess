# Intentionally empty. Browser cache for Laravel lives in public/.htaccess.
# Do not use AddOutputFilterByType here — XAMPP often lacks mod_filter/mod_deflate.
<IfModule mod_rewrite.c>
    RewriteEngine On

    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>